


export function taskDataObj (title, date, details) {

    return {
        title: title || 'title',
        date: date || 'date',
        details: details || 'details'
    }

}

export let dataRetrieve = [];